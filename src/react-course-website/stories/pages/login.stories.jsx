import React from 'react';

import { Login } from '../../layout/login';

export default {
  title: 'Pages/Login component',
  component: Login
};

export const loginPage = () => <Login handleLogin={() => {}} />;
